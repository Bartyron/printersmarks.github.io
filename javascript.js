
//FIRST TIME USING JAVASCRIPT!!!

var id;
function open_close(id){

    //get the element we decided to use
    var popup = document.getElementById(id);

    //show it
    popup.classList.toggle("show");

    //document.getElementById("close").style.marginTop = "150px";
}

function close(id){

    //get the element we decided to use
    var popup = document.getElementById(id);

    //show it
    popup.classList.toggle("show");
}

